<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- Logo - Independent positioning -->

    <!-- Sign Up Title - Independent positioning -->

    <ImageView
        android:id="@+id/logo"
        android:layout_width="265dp"
        android:layout_height="243dp"
        android:layout_marginTop="28dp"
        android:contentDescription="Logo"
        android:src="@drawable/white_background_logo"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.39"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/title_sign_up"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Sign Up Title"
        android:src="@drawable/sign_up"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="20dp"
        android:layout_marginTop="260dp" />

    <!-- Email Label - Independent positioning -->
    <TextView
        android:id="@+id/email_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Email"
        android:textColor="#000000"
        android:textSize="16sp"
        android:textStyle="bold"
        android:fontFamily="serif"
        android:letterSpacing="-0.022"
        android:lineSpacingMultiplier="1.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="20dp"
        android:layout_marginTop="308dp" />

    <!-- Email Input Container - Independent positioning -->
    <FrameLayout
        android:id="@+id/email_container"
        android:layout_width="359dp"
        android:layout_height="48dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="20dp"
        android:layout_marginTop="340dp">

        <!-- Black Textbox Background -->
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="Email Background"
            android:scaleType="fitXY"
            android:src="@drawable/black_textbox" />

        <!-- Email EditText -->
        <EditText
            android:id="@+id/email_input"
            android:layout_width="335dp"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:hint="Enter your email"
            android:inputType="textEmailAddress"
            android:maxLines="1"
            android:paddingStart="15dp"
            android:paddingEnd="15dp"
            android:textColor="@android:color/white"
            android:textColorHint="@android:color/darker_gray"
            android:textSize="16sp" />
    </FrameLayout>

    <!-- Username Label - Independent positioning -->
    <ImageView
        android:id="@+id/username_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Username Label"
        android:src="@drawable/username"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="20dp"
        android:layout_marginTop="408dp" />

    <!-- Username Input Container - Independent positioning -->
    <FrameLayout
        android:id="@+id/username_container"
        android:layout_width="359dp"
        android:layout_height="48dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="20dp"
        android:layout_marginTop="440dp">

        <!-- Black Textbox Background -->
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="Username Background"
            android:scaleType="fitXY"
            android:src="@drawable/black_textbox" />

        <!-- Username EditText -->
        <EditText
            android:id="@+id/username_input"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:hint="Enter a username"
            android:inputType="text"
            android:maxLines="1"
            android:paddingStart="15dp"
            android:paddingEnd="15dp"
            android:textColor="@android:color/white"
            android:textColorHint="@android:color/darker_gray"
            android:textSize="16sp" />
    </FrameLayout>

    <!-- Password Label - Independent positioning -->
    <ImageView
        android:id="@+id/password_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Password Label"
        android:src="@drawable/password"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="20dp"
        android:layout_marginTop="508dp" />

    <!-- Password Input Container - Independent positioning -->
    <FrameLayout
        android:id="@+id/password_container"
        android:layout_width="359dp"
        android:layout_height="48dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="20dp"
        android:layout_marginTop="540dp">

        <!-- Black Textbox Background -->
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="Password Background"
            android:scaleType="fitXY"
            android:src="@drawable/black_textbox" />

        <!-- Password EditText -->
        <EditText
            android:id="@+id/password_input"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:hint="Enter a password"
            android:inputType="textPassword"
            android:maxLines="1"
            android:paddingStart="15dp"
            android:paddingEnd="15dp"
            android:textColor="@android:color/white"
            android:textColorHint="@android:color/darker_gray"
            android:textSize="16sp" />
    </FrameLayout>

    <!-- Account Link - Independent positioning -->
    <TextView
        android:id="@+id/account_link"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:focusable="true"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:letterSpacing="-0.019"
        android:lineSpacingMultiplier="1.5"
        android:text="Already have an account? Sign In"
        android:textColor="#8A7F7F"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="608dp" />

    <!-- Sign Up Button Container - Independent positioning -->
    <FrameLayout
        android:id="@+id/sign_up_button_container"
        android:layout_width="200dp"
        android:layout_height="50dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="648dp">

        <!-- Yellow Button Background -->
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="Sign Up Button Background"
            android:scaleType="fitXY"
            android:src="@drawable/sign_in_button" />

        <!-- Sign Up Button -->
        <Button
            android:id="@+id/sign_up_button"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:text="Sign Up"
            android:textColor="#000000"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="-0.011"
            android:gravity="center"
            android:lineSpacingExtra="8sp" />
    </FrameLayout>

    <!-- Or Sign Up Using Text - Independent positioning -->
    <ImageView
        android:id="@+id/or_sign_up_using"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Or Sign Up Using"
        android:src="@drawable/or_sign_up_using"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="728dp" />

    <!-- Google Icon Button - Independent positioning -->
    <ImageButton
        android:id="@+id/google_button"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:background="@android:color/transparent"
        android:contentDescription="Google Sign Up"
        android:scaleType="fitCenter"
        android:src="@drawable/google_icon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="155dp"
        android:layout_marginTop="768dp" />

    <!-- GitHub Icon Button - Independent positioning -->
    <ImageButton
        android:id="@+id/github_button"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:background="@android:color/transparent"
        android:contentDescription="GitHub Sign Up"
        android:scaleType="fitCenter"
        android:src="@drawable/github_icon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="225dp"
        android:layout_marginTop="768dp" />


</androidx.constraintlayout.widget.ConstraintLayout>